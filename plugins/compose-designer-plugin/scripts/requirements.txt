# scrcpy-helper dependencies (Python 3.11+ required)
# Install in venv: scripts/scrcpy_helper/.venv/
#
# Setup:
#   cd scripts/scrcpy_helper
#   python3.12 -m venv .venv
#   .venv/bin/pip install mysc Pillow numpy adbutils

mysc
Pillow>=10.0.0
numpy>=1.24.0
adbutils
